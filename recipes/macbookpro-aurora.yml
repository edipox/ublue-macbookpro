name: macbookpro-aurora
description: Custom image for MacBook Pro 13,1 using Aurora
base-image: ghcr.io/ublue-os/aurora
image-version: stable-daily # latest is also supported if you want new updates ASAP
modules:
  - from-file: common/common-setup.yml
  - type: signing
